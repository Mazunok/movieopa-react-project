export enum ROUTE {
  HOME = "/",
  TRANDS = "/trands",
  FAVORITES = "/favorites",
  SETTINGS = "/settings",
  SEARCH = "/search/:page",
  SIGN_IN = "/sign-in",
  SIGN_UP = "/sign-up",
  NOT_FOUND = "*",
  MOVIE_DETAILS = "/movie/:id",
  CHANGE_PASSWORD = "/change-password",
  LOG_OUT = "log-out",
}
