export enum ROUTES {
  HOME = "/",
  TRANDS = "/trands",
  FAVORITES = "/favorites",
  SETTINGS = "/settings",
  SEARCH = "/search",
  SIGN_IN = "/sign-in",
  SIGN_UP = "/sign-up",
  NOT_FOUND = "*",
  MOVIE_DETAILS = "/movie/:id",
  CHANGE_PASSWORD = "/change-password",
}
