export enum routes {
  HOME = "/",
  TRANDS = "/trands",
  FAVORITES = "/favorites",
  SETTINGS = "/settings",
  SEARCH = "/search",
  SING_IN = "/singin",
  SING_UP = "/singup",
  NOT_FOUND = "*",
}
